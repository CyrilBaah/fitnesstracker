apiVersion: apps/v1
kind: Deployment
metadata:
  name: fitnesstracker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fitnesstracker
  template:
    metadata:
      labels:
        app: fitnesstracker
    spec:
      containers:
        - name: k8s-web
          image: cyrilbaah/fitnesstracker
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: fitnesstracker
          env:
            - name: SECRET-KEY
              valueFrom:
                secretKeyRef:
                  name: fitnesstracker
                  key: secret-key
            - name: DB-PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fitnesstracker
                  key: db-password
            - name: DB-HOST
              valueFrom:
                secretKeyRef:
                  name: fitnesstracker
                  key: db-host



            
